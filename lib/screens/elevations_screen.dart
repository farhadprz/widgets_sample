import 'package:flutter/material.dart';

class ElevationsScreen extends StatelessWidget {
  const ElevationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Elevations Screen'),);
  }
}
