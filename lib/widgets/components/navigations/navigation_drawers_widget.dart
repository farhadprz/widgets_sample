import 'package:flutter/material.dart';

class NavigationDrawersWidget extends StatelessWidget {
  const NavigationDrawersWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
